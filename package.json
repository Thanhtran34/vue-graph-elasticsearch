{
  "name": "assignment-wt2-big-data",
  "version": "1.0.0",
  "description": "assignment for testing elasticsearch",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "start:dev": "nodemon --inspect -r dotenv/config src/server.js",
    "lint": "npx eslint ./src || exit 0",
    "lint:fix": "npx eslint ./src --fix || exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.lnu.se:1dv027/student/tt222kj/assignment-wt2-big-data.git"
  },
  "author": "Thanh Tran",
  "license": "MIT",
  "devDependencies": {
    "dotenv": "^16.0.0",
    "eslint": "^8.13.0",
    "nodemon": "^2.0.15"
  },
  "nodemonConfig": {
    "ext": "js,json,hbs,html,css"
  },
  "dependencies": {
    "@elastic/elasticsearch": "^7.17.0",
    "csvtojson": "^2.0.10",
    "express": "^4.17.3"
  }
}
